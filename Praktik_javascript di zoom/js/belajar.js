let person;
person = 'yanti';

alert(person);
const nomor1 = 20

console.log(nomor1 + 10)
let nomor2 = 20; // Deklarasikan dan beri nilai awal pada variabel `nomor2`
console.log(nomor2 + 10); // Menggunakan `nomor2` setelah deklarasi

function fu() {
    console.log("Hello");
}
fu(); // Memanggil fungsi `fu` setelah dideklarasikan


if (true) {
    var pekerjaan = "Programmer";
    let hobi = "Membaca";
    console.log(pekerjaan); // Output: Programmer
    console.log(hobi);      // Output: Membaca
}

console.log(pekerjaan); // Output: Programmer (var dapat diakses di luar blok)

function findAngka(num1, num2) {
    // Logika fungsi di sini
    return num1 + num2;
}

